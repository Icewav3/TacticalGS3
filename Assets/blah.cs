using UnityEngine;

public class blah : MonoBehaviour
{
	private int an_integer = 5;
	// Start is called before the first frame update
	void Start ( ){
		var A=0;
        if(an_integer>=A){
            A=an_integer;
		}
	}

	// Update is called once per frame
	void    Update()
	{

	}
}